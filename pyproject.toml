[project]
name = "unsubscribely-algokit"
version = "0.1.0"
description = "AlgoKit backend layer for Unsubscribely — Algorand smart contracts for escrow vaults"
requires-python = ">=3.12"
dependencies = [
    "algorand-python>=2.0.0",
    "algokit-utils>=3.0.0",
    "py-algorand-sdk>=2.0.0",
    "python-dotenv>=1.0.0",
    "pytest>=8.0.0",
]
