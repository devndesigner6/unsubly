FROM node:20-alpine

WORKDIR /app

# Copy agent package and install deps
COPY agents/openclaw/package.json ./
RUN npm install --production

# Copy agent source
COPY agents/openclaw/ ./

# Run the persistent agent loop
CMD ["node", "agent-loop.mjs"]
